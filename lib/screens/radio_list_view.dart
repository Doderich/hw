//import 'package:flutter/material.dart';
//import 'package:hw/screens/main_screen.dart';

//enum classes {Klasse5, Klasse6, Klasse8}
/*
class RadioListView extends StatelessWidget {
  RadioListView(this._group);
  final _group;
  Enum classes = _group;

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(color: Colors.pink[200]),
      constraints: BoxConstraints(
        maxWidth: 200,
        maxHeight: 100,
      ),
      child: ListView.builder(
          itemCount: 3,
          itemBuilder: (context, index) {
            return RadioListTile(
                title: Text(classes.values[index].toString()),
                controlAffinity: ListTileControlAffinity.trailing,
                value: classes,
                groupValue: _group,
                onChanged: ((classes? value) {
                  print('object');
                }));
          }),
    );
  }
}
*/
